<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dealreveal">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_MEDIA_LOCATION" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DealReveal"

        >

        <activity android:name=".Activites.admins.AdminbusinesschangeActivity"></activity>
        <activity android:name=".Activites.client.Businessupdate2Activity" />
        <activity android:name=".Activites.client.Businessupdate1Activity" />
        <activity android:name=".Activites.client.UpdateBusinessInfoActivity" />
        <activity android:name=".Activites.client.clientreviewfeedbackActivity" />
        <activity android:name=".Activites.admins.LiarreportActivity" />
        <activity android:name=".Activites.shared.DealfeedbacktActivity" />
        <activity android:name=".Activites.client.singledealanalyticsinfoActivity" />
        <activity android:name=".Activites.client.AnalyticsAllDealActivity" />
        <activity android:name=".Activites.users.UsersettingActivity" />
        <activity android:name=".Activites.shared.ClientCollectionDealActivity" />
        <activity android:name=".Activites.users.BusinessrevealwithmapActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".Activites.users.Buisnessreveal"
            android:label="@string/title_activity_buisnessreveal" />
        <activity android:name=".Activites.users.DealswipeActivity" />
        <activity android:name=".Activites.UserSavedDealsActivity" />
        <activity android:name=".Activites.users.DealRevealUserActivity" />
        <activity android:name=".Activites.users.DealRevealfilterActivity" />
        <activity android:name=".Activites.client.ClientsettingsActivity" />
        <activity android:name=".Activites.DealCollectionViewActivity" />
        <activity android:name=".Activites.shared.RejectDealReasonActivity" />
        <activity android:name=".Activites.PendingapprovalActivity" />
        <activity android:name=".Activites.client.DealRevealActivity" />
        <activity android:name=".Activites.client.PostNewDealInfoActivity" />
        <activity android:name=".Activites.client.InitalpostnewdealActivity" />
        <activity android:name=".Activites.client.ForgotpasswordActivity" />
        <activity android:name=".Activites.client.BusinessSigninActivity" />
        <activity android:name=".Activites.admins.possibleclient3Activity" />
        <activity android:name=".Activites.admins.possibleclient2Activity" />
        <activity android:name=".Activites.admins.PossibleClient1Activity" />
        <activity android:name=".Activites.admins.ApprovedealsActivity" />
        <activity android:name=".Activites.AdminApproveClients" />
        <activity android:name=".Activites.admins.AdminhomeActivity" />
        <activity android:name=".Activites.admins.AdminloginActivity" />
        <activity android:name=".Activites.admins.HodorActivity" />
        <activity android:name=".Activites.client.FinalBusinessSignupActivity" />
        <activity android:name=".Activites.client.BusinessSignup3Activity" />
        <activity android:name=".Activites.client.BusinessSignup2Activity" />
        <activity android:name=".Activites.client.BusinessSignup1Activity" />
        <activity android:name=".Activites.users.UsersignupsecondActivity" />
        <activity android:name=".Activites.users.UserSignUpActivity" />
        <activity android:name=".Activites.shared.HelpOverviewActivity" />
        <activity android:name=".Activites.shared.HelpReminderActivity" />
        <activity
            android:name=".Activites.users.LoginActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity android:name=".Activites.shared.Startscreen" />
        <activity
            android:name=".Activites.shared.SplashActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            android:exported="true">

            <!-- android:theme="@style/AppTheme" -->

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            >
        </activity>

        <receiver
            android:name=".Activites.users.AlarmReceiver"
            android:enabled="true" />
        <receiver android:name=".Activites.shared.receiver.AlarmReceiver" />

        <service
            android:name=".Activites.users.NotificationService"
            android:enabled="true" />

        <activity
            android:name=".Activites.users.ResultActivity"
            android:excludeFromRecents="true"
            android:launchMode="singleTask"
            android:taskAffinity="" />
    </application>

</manifest>